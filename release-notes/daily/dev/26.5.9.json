{
  "channel": "dev",
  "dayKey": "26.5.9",
  "date": "2026-05-09",
  "preferredDeck": "Adds renderer surface metrics, reduces Settings frame drops, and keeps the same-day performance fixes together.",
  "editorialGuidance": [
    "Keep the tone concise, professional, and specific.",
    "Lead with user-facing outcomes, shipping milestones, trust wins, and installability improvements.",
    "Demote internal cleanup unless it clearly changes the shipped product."
  ],
  "pinnedHighlights": [
    "Feed and Map location scans now prefilter text before running location regexes, preserving supported text-location matches while avoiding needless regex work on ordinary article text.",
    "Settings now avoids forcing its scroll container into a promoted compositor layer and keeps the dialog shell blur stable during movement, reducing compositor churn while preserving the blurred backdrop.",
    "Friends Fit all now keeps sparse graph nodes visible instead of clamping zoom too high for far-apart people.",
    "Settings avoids repeated scroll timer churn and uses visibility observation for the update auto-check, reducing interaction work while the dialog is moving.",
    "Expected hidden-window WebKit timer throttling now records renderer_heartbeat_throttled instead of renderer_heartbeat_stale, so installed-build soaks stop reporting fake stale and recovered cycles.",
    "Renderer heartbeats now include the active surface plus Friends graph and Map counts, making installed build soaks attribute memory and frame issues to the visible product area.",
    "Settings avoids repeated document style invalidation during scroll and search, keeps blur suppression through the interaction tail, and isolates its scrollport to reduce dropped frames.",
    "Dense Map views now mount 160 markers instead of 240 and only paint 80 while moving, giving the 1,600 author Map guard more frame headroom on loaded runners.",
    "Settings now lazy-mounts section bodies, suppresses expensive blur while actively scrolling or searching, and has a dedicated 1,600-source performance guard.",
    "Feed cards now shed inline media when aggregate WebKit RSS reaches 1.5 GB, which matches the pressure range seen in the live dev soak before the old 2 GB threshold fired.",
    "Friends graph Fit all now waits for dense layout completion before applying.",
    "Dense Map fallback rendering reduces visible marker paint work during wheel and pointer movement.",
    "Shows the fatal recovery screen instead of a blank shell when a React render error is caught.",
    "Restores WebKit WebContent memory attribution for current launch renderer processes.",
    "Relaxes hidden renderer watchdog recovery so hidden or backgrounded windows are not recycled too aggressively.",
    "Improves dense Friends graph recovery, Map interaction smoothness, desktop crash recovery, and WebKit memory attribution for today's dev builds",
    "Show crash recovery when rendering fails",
    "Stabilize map markers across refreshes",
    "Relax hidden renderer watchdog",
    "Keeps hidden sessions quieter and restores WebKit memory attribution",
    "Reduces dense Map marker work, speeds Settings, and sheds feed media earlier under WebKit pressure",
    "Reduce settings dialog frame work",
    "Friends graph Fit all now waits for the first dense layout to finish before applying, so large graphs no longer get stuck off-screen when the action fires early"
  ],
  "editorialNotes": [
    "Latest dev build replaces failed v26.5.904-dev and adds the Map marker cap fix on top of the earlier same-day performance work."
  ],
  "updatedAt": "2026-05-10T02:51:14.881Z"
}
