(AI Generated).

## Freed v26.8.1002-dev

The PWA now reads and searches the SQLite Library while Freed Desktop accepts signed mobile intents.

### Features

- Load, page, search, and open the complete SQLite Library from PWA IndexedDB
- Publish signed PWA read-state intents and commit them through Freed Desktop's SQLite authority
- Transfer the single Freed Desktop writer epoch with an exact Google Drive compare-and-swap

### Fixes

- Refresh the authenticated immutable checkpoint after accepting PWA intents
- Keep full-library PWA search and reader content off the legacy Automerge worker

### Follow-ups

- Publish explicit PWA acceptance and provider-result receipts
- Add the user-visible 24-generation cloud backup mirror
- Finish the reviewed replacement-sync activation and retire Automerge

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
