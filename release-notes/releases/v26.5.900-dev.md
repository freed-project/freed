(AI Generated).

## Freed v26.5.900-dev

Hidden sessions stay quieter

### Fixes

- Reduces hidden-window watchdog diagnostics during normal WebKit timer throttling, which cuts avoidable background work during locked or hidden sessions while keeping foreground renderer recovery unchanged.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
