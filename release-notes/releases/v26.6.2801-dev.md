(AI Generated).

## Freed v26.6.2801-dev

Renderer memory recovery stability, freed Desktop now destroys and rebuilds the main WebView when idle WebKit memory is reclaimable but still resident, and renderer recovery now ignores stale age-matched WebKit processes after a rebuild, which prevents repeated recovery loops and unnecessary background safe mode

### Fixes

- Bug fixes and improvements

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
