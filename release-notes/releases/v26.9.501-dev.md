(AI Generated).

## Freed v26.9.501-dev

Give large Drive publications a bounded progress budget and preserve pending provider syncs

### Features

- An interactive sample Library demo

### Fixes

- Publish larger SQLite Libraries without the stale receipt-count limit
- Scale the publication deadline to the pinned export while stopping attempts that make no useful progress for five minutes
- Optimized checkpoint encoding work without changing canonical bytes
- Keep scheduled provider syncs pending while verified writer admission is unavailable
- Persistent diagnostics for the Freed Desktop main window restored
- Keep local snapshot exports off the main thread so they do not block the interface or provider WebViews
- Seek directly to each checkpoint page instead of rescanning previously exported records
- Coalesce overlapping Drive publications and retain the export slot until canceled native work settles
- Recognize unlocked macOS sessions and accept fractional provider wake deadlines
- Distinguish missing or unreadable writer admission from a retired writer
- Treat a PWA Library held by another tab as busy rather than corrupted
- Preserve release lane controls and Library Core activation history

### Follow-ups

- Verify Drive publication, writer admission, and LinkedIn extraction on the signed build
- Scheduled Facebook and Instagram observation on the installed build
- PWA clients before importing checkpoints above the former receipt-count limit, then verify bidirectional sync

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
