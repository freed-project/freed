(AI Generated).

## Freed v26.7.203-dev

Map time range slider, Friends graph lane interactions restored, and graph pin release validation are now more stable

### Features

- Map time range slider

### Fixes

- Release validation now removes a stale Azure CLI apt source before installing Playwright browser dependencies on GitHub-hosted Ubuntu runners

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
