(AI Generated).

## Freed v26.7.2600-dev

The first dev build in eleven days, carrying 62 commits of storage and memory work

### Features

- The cloud merge now runs in a worker that is terminated after each merge, so its peak no longer becomes a permanent floor
- Renderer memory is attributed against a pre-hydration baseline, so growth can be traced to a cause rather than guessed at
- Archive pruning runs again for cloud-synced libraries, where startup maintenance had never run at all

### Fixes

- Cloud sync bodies cross the native boundary as raw bytes instead of a JSON number array
- Feed ranking is split into a time-invariant part and a live recency term, and no longer syncs between devices
- Every synced field now declares where it lives and what happens when it is deleted
- Feed items can be projected into flat rows, proved lossless against a real 15,846-item library
- Moved off a Rust HTTP dependency that had been fully yanked upstream
- The tooling lane runs only suites a change can reach, and warns before scheduling work that cannot finish in time
- Sidebar label measurement in the zoom test no longer reads geometry mid-animation
- Roadmap layout ref syncs after commit rather than during render
- Historical cloud promotions are recognised again
- Facebook group regression aligned with local state

### Known limits

- This build is not expected to unblock Facebook and Instagram. Those are refused when the renderer sits above a 4 GB scrape budget, and moving the library out of the renderer is still ahead
- Reading the memory telemetry from this build is what tells us whether the changes above moved the renderer at all

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
