(AI Generated).

## Freed v26.9.901-dev

Readable map tooltips, restored bookmarks, and more reliable checkpoint recovery

### Features

- Explore the 500-post demo with character-specific feeds and a full-screen mobile welcome
- Read unified story and post cards with consistent spacing, interface zoom, and paused YouTube playback
- Select friends across the map, galaxy, and sidebar, and open provider-filtered reading lists

### Fixes

- Map tooltips retain their theme background and readable text after MapLibre loads, across all six themes
- Returning from the reader after resizing no longer opens a fatal-error screen when the browser skips its transition animation
- Saved shows the complete bookmark collection even when the main feed is filtered to Friends
- Reader controls use consistent theme styles, expose bookmark state, and keep column switching accessible
- Facebook login completion uses bounded checks and recognizes existing feed evidence when cookies are not script-readable
- Provider Advanced controls remain accessible while disconnected or awaiting reconnect
- PWA checkpoints survive large imports and worker loss, and later retries can resume an existing checkpoint stage
- PWA reset, scrolling, and Drive Library selection work again; SQLite integrity and timeout failures have clearer diagnostics
- Dev releases no longer schedule a production marketing-site deployment

### Follow-ups

- Facebook login completion still needs verification after an owner-completed login on this signed build

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
