(AI Generated).

## Freed v26.6.101-dev

Facebook, Instagram, and LinkedIn sync now share the same lower-profile feed scrolling behavior. Facebook no longer treats an unauthenticated or stalled page as a clean zero-post sync, and social source status no longer shows old posts as proof of a live connection.

### Fixes

- Facebook sync now reports unauthenticated or stalled feed pages as real sync failures instead of a successful zero-post pull.
- Social source status now requires live auth state before showing a connected badge, so old items do not mask a lost provider session.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
