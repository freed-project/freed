(AI Generated).

## Freed v26.6.2502-dev

Freed now ships the app-recovery sync cleanup with the latest desktop stability and release-validation fixes.

### Fixes

- Cleaned up app recovery deferrals so Facebook, Instagram, and LinkedIn no longer show raw millisecond safe-mode errors in provider status.
- Aligned LinkedIn feed sync with the shared social scrape queue so app recovery pauses do not poison provider health or stop later retries.
- Reduced Friends graph interaction churn and stabilized desktop window compositing during recovery.
- Pinned release script Node routing and relaxed only the scripted settings scroll perf buffer that blocked the previous dev build.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
