(AI Generated).

## Freed v26.5.1008-dev

Replacement dev build for the hidden renderer heartbeat fix and Friends performance work.

### Fixes

- Fixed the Rust release build path for hidden renderer heartbeat throttling so the replacement dev build can package on every platform.
- Hidden WebKit timer gaps are still the focus of the installed-build soak. The source fix should stop expected hidden timer throttling from turning into recurring false stale-renderer cycles once this build is installed.
- Settings scrolling is lighter, and Friends view transitions are stable through the release regression gate.

### Performance

- Friends dense graph interaction keeps the cached motion layer during pan and zoom, reducing redraw work for 1,600 visible connections.
- Desktop performance validation remains green for Feed, Friends, Map, Source sidebar, and Settings. Settings is still the next measured surface because scroll p95 remains right around the 50 ms budget.

### Validation

- Replaces failed draft tag `v26.5.1007-dev`, which passed validation but failed packaging because a heartbeat threshold constant was accidentally test-only.
- Active installed-build soak remains passive while macOS Apple Events authorization is denied, so no click, scroll, zoom, or content automation is counted from the current local run.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
