(AI Generated).

## Freed v26.5.1003-dev

Settings scrolling is lighter, and Friends view transitions are stable through the release regression gate.

### Performance

- Pauses Settings descendant transitions while the dialog is moving so scroll and drag do less compositor work.
- Keeps the Settings scroll containment fixes from the previous dev builds.
- Preserves the Friends graph scale improvements already in the installed soak line.

### Fixes

- Opens the full Map from a Friends last seen card with one atomic store update.
- Flushes the Friends to Map handoff at the click boundary so release CI cannot observe Map state before the Map surface commits.
- Flushes mobile Friends toolbar switches between Details and graph modes so the child surface commits before release assertions run.

### Validation

- Replaces v26.5.1000-dev, v26.5.1001-dev, and v26.5.1002-dev after release validation exposed Friends transition races.
- PR feature validation passed for both Friends transition fixes.
- The final mobile Friends Details case passed 30 repeated local runs before this build was cut.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
