(AI Generated).

## Freed v26.5.1001-dev

Settings scroll performance and Friends Map handoff stability.

### Performance

- Isolates the Settings shell and scrollport with CSS containment, which reduces paint spillover while the dialog is open.
- Pauses descendant animations and transitions while Settings is actively moving, which trims extra frame work during scroll and interaction.

### Fixes

- Stabilizes the Friends detail last-seen Map handoff by switching to the full Map view and focused person in one store transition.
- Updates the regression to click the last-seen card through Playwright actionability instead of firing a DOM click from a polling predicate.

### Validation

- Focused Settings performance stayed inside guard with no long tasks.
- The Friends detail to Map smoke case passed 20 repeated focused runs after the handoff fix.
- Feature validation passed before release prep.

### Downloads

**macOS:** `.dmg` Apple Silicon or Intel, signed and notarized  
**Windows:** `.exe` NSIS installer  
**Linux:** `.AppImage`

> macOS downloads are signed and notarized for normal Gatekeeper installation.
