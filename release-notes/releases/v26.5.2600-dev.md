(AI Generated).

## Freed v26.5.2600-dev

Keeps dense Map interactions responsive while panning and zooming large location sets.

### Fixes

- Keeps the fallback Map view from restoring all dense markers too early during motion, which was pushing frame times over budget in release validation.
- Includes the dense marker culling fix from the earlier failed dev build, so Map keeps only the highest-priority visible markers attached while the view is moving.
- Pins the event metadata unit test clock so release validation stays deterministic after the real calendar moves past a fixture date.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
