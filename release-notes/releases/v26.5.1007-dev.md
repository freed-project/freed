(AI Generated).

## Freed v26.5.1007-dev

Settings scroll, Friends graph, and renderer recovery stability.

### Fixes

- Pause Settings descendant transitions while the dialog is moving.
- Open the full Map from Friends detail with one atomic store update.
- Flush the Friends to Map handoff at the click boundary.
- Flush mobile Friends toolbar switches between Details and graph modes.
- Cache the dense Friends graph interaction layer and add rebuild-count telemetry.
- Keep hidden WebKit timer gaps classified as expected throttling instead of recurring false stale-renderer cycles.
- Ignore Desktop Playwright output directory events so release E2E artifacts cannot reload the app under test.
- Use a CI-specific identity graph benchmark budget while keeping the local 2,000 ms layout guard intact.

### Follow-ups

- Settings scrolling is lighter, and Friends view transitions are stable through the release regression gate
- Install this replacement build locally and confirm hidden timer gaps report as renderer_heartbeat_throttled without recurring false renderer_heartbeat_stale cycles.
- Continue passive soak metrics until Apple Events authorization is restored for active UI driving.
- Treat Settings scroll as the next measured performance surface because it remains the largest budget miss.

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
