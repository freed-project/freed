(AI Generated).

## Freed v26.8.2002-dev

SQLite Library contracts, a provider-free primary Library, and bounded Drive checkpoints

### Features

- Generate SQLite Library contracts
- Descriptor-bound Library sidecar
- Fail-closed Library service supervisor

### Fixes

- Enforce PWA actor capabilities
- Support local-only Library authority
- Bound Drive checkpoint pages by decoded bytes
- Defer checkpoint refresh until the current render settles
- Preserve PWA intents across checkpoint activation
- Bound Library Core search work and result size
- Repair Drive mutable revision fencing
- Enforce one Library Core process per data root
- Recognize release identity backflow
- Keep SQLite backups off cloud storage
- Show honest Drive sync activity
- Read immutable Drive objects without browser ETags
- Prevent Drive sync lifecycle stalls
- Stabilize Drive Library Core bootstrap
- Route CI away from stalled Ubuntu mirrors

### Follow-ups

- Publish normalized typed Library checkpoints
- Import Library checkpoints into SQLite WASM over OPFS
- Retire remaining compatibility shell reads

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
