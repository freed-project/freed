(AI Generated).

## Freed v26.7.2703-dev

Project feed items to flat rows with a losslessness proof, the locality and deletion contract, and sync, cloud, and pairing infrastructure

### Features

- Project feed items to flat rows with a losslessness proof
- The locality and deletion contract
- Attribute renderer memory against a pre-hydration shell baseline

### Fixes

- Release build and type-safety cleanup
- Sync the roadmap layout ref after commit instead of during render
- Defer archive pruning to cloud reconciliation instead of skipping it
- Registry test filesystem race removed
- Recognize historical cloud promotions
- Align Facebook group regression with local state
- Stabilizes local AI health, runtime health, and renderer recovery accounting
- Hardens automation control, release validation, and dependency safety
- Keeps Friends Galaxy labels live during motion

### Follow-ups

- Moves cloud merges off the main thread and replaces JSON number arrays with compact IPC bodies
- Lays the measured foundation for moving the corpus out of the renderer, reduces cloud merge overhead, and hardens sync and automation
- Facebook and Instagram may still pause at the WebKit memory gate. The corpus remains resident in Automerge until the storage migration removes it from the renderer

### Downloads

**macOS:** `.dmg` (Apple Silicon or Intel, signed + notarized)  
**Windows:** `.exe` (NSIS installer)  
**Linux:** `.AppImage`  

> macOS downloads are signed and notarized for normal Gatekeeper installation.
