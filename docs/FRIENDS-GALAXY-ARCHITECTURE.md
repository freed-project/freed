# Friends Galaxy Architecture

Status: Product cutover implementation and integrated acceptance are complete as of July 19, 2026. Dev release publication is pending.

## Historical Summary

PR 746 replaced the retained Pixi full-scene Friends graph with a summary-backed atlas worker and a theme-aware Three.js starfield. It added Troika billboard labels, capped visible nodes, context-menu linking and pinning, provider nebula and ring variants, prominence depth, touch gestures, diagnostics, and a canvas fallback.

This implementation is a useful prototype, but it is not the final FriendsGraph architecture. The approved direction below supersedes the visual and rendering assumptions in the original implementation while preserving its product workflows and lessons.

## Prototype Findings At Kickoff

- The current layout is fundamentally two-dimensional. Depth is added after the atlas layout, so the scene is layered 2D rather than a true 3D spatial model.
- Zoom and pan scale a scene group instead of moving a camera over a galactic plane.
- Interactive quality gains frames by removing labels, regions, edges, background stars, and most semantic nodes. This makes movement less informative and visually unstable.
- The worker sends rich JavaScript objects and viewport slices instead of a compact renderer-neutral typed scene.
- Region meshes and Troika label objects are destroyed and recreated during synchronization.
- Hit testing ignores projected depth and rebuilds transient lookup structures.
- Whole-library derivation remains too expensive, including repeated account filtering per person and mount-time summary work.
- The same DOM element was used for visual background bleed and pointer interaction. PR 887 had to retract under-sidebar bleed to restore empty-space clicks and control alignment.
- More than twenty graph-specific performance and stability patches before and after PR 746 demonstrate architectural saturation rather than one missing optimization.

## Approved Product Vision

FriendsGraph will become a navigable personal galaxy. It must remain understandable at every zoom level, respond directly to touch, preserve stable geography across sessions, and render a large library without hiding the galaxy whenever the user moves.

The graph occupies a constrained galactic plane. `x` and `y` encode stable semantic geography. `z` encodes importance and prominence, placing important people visually closer to the camera. Depth is deliberate and bounded rather than random.

## Approved Spatial Model

| Signal | Spatial or visual encoding |
|---|---|
| `x/y` | Stable semantic geography on the galactic plane |
| `z` | Prominence, with important people closer to the camera |
| Star size | Relationship tier and identity significance |
| Brightness | Recent activity and current relevance |
| Pulse or corona | Reach-out due state, selection, or new activity |
| Color | Active theme plus consistent semantic and provider roles |
| Local orbit | Accounts and feeds associated with one identity |

- Fam and the most important confirmed friends occupy the core.
- Confirmed friends form the inner disk, arranged by care, activity, and relationship neighborhoods.
- Provisional connection identities occupy the middle disk.
- Each person forms a tight local solar system. Linked accounts orbit nearby with short, bright connectors.
- Unlinked accounts and feeds occupy stable provider sectors on the outer rim.
- Instagram, Facebook, LinkedIn, X, RSS, and future providers receive distinct, theme-compatible constellations.
- Overdue Fam contacts use a reconnect aurora or highlighted sector. Their permanent geography does not jump when due state changes.
- Suggested identities appear as uncertain candidate stars and never auto-promote or auto-link.
- Placement is deterministic. Incremental data changes must not rearrange the galaxy.

## Approved Visual Contract

- Every active Freed theme controls the whole scene. The starfield is not dark-mode-only.
- Stars are large, legible, and meaningful at the initial fitted view.
- The first view exposes important people, major provider geography, and useful labels.
- Labels are billboard text rendered in a batched GPU SDF or MSDF layer, not an HTML cloud or one scene object per label.
- Avatars appear at close zoom for selected or high-priority identities and load after motion settles through a texture atlas.
- Links remain short and visible. Global hairball edges are forbidden.
- Nebula is the default provider-field treatment. Constellation streams and a combined treatment remain switchable in a development comparison control.
- Depth creates restrained, perceptible parallax without sacrificing navigation clarity.
- The visual render layer fills beneath the primary app sidebar, while the pointer interaction lane and controls remain bounded between sidebars.
- Reduced-motion settings disable pulses and settle animation without removing information.

## Approved Interaction Contract

- V1 uses a perspective camera with locked rotation and no camera roll.
- One finger pans. Two fingers pan and pinch around their midpoint. Wheel and trackpad use the same camera model, with focal-point-preserving pinch momentum after a Mac trackpad release.
- Pinch uses ray-to-plane math so the world point beneath the midpoint remains fixed.
- Tap selects. Double tap focuses. Empty-space tap clears the collapsed detail card.
- Long press or right click opens the node context menu.
- Pinning happens only from the node context menu.
- Account linking opens a searchable person picker from the context menu.
- Promotion to Followed, Friends, or Fam remains explicit.
- Drag never links or pins. Normal drag is navigation.
- Fit All, search focus, reader author links, and Map links focus the same canonical identity.
- Desktop retains the resizable and collapsible detail rail. Mobile retains the separate Details lens.

## Approved Data and Workflow Contract

- Preserve canonical `Person` plus attached `Account` records.
- Preserve Friends and All Content modes.
- Preserve provisional identities, provider sectors, RSS, suggestions, persistent pins, relationship tiers, reach-out history, and selected cross-platform timelines.
- Suggestions remain local, ranked, explainable, dismissible, and confirmation-only.
- Full item arrays load only for the selected identity. The graph consumes incrementally maintained activity summaries.
- Reader, search, Friends, and Map continue to share identity navigation.
- Rendering remains local and adds no provider navigation, requests, scraping, or provider-visible behavior.

## Approved Rendering Direction

- Build an imperative `GraphEngine` with no React scene graph and no per-frame React state.
- Use a renderer-neutral typed scene protocol so the same graph compiler can feed multiple GPU backends.
- Run an engineering bakeoff between raw WebGPU/WGSL and Three.js `WebGPURenderer` with TSL and instanced buffers.
- Use WebGPU as the preferred shared renderer and WebGL2 as the compatibility backend.
- Do not use React Three Fiber or Drei for the core scene. Drei Text wraps Troika and does not solve large batched label rendering.
- Keep native Metal as a benchmark-gated backend option. Add it only if the shared WebGPU implementation cannot meet installed-device budgets.
- Keep every semantic star GPU-resident. LOD applies to labels, avatars, edge detail, picking, and expensive effects, not to the existence of the galaxy during motion.

## Approved Performance Contract

- Keep at least 30,000 semantic graph stars GPU-resident, matching 5,000 people plus 25,000 accounts.
- Support at least 100,000 decorative background stars in one instanced pass.
- First visual frame under 400 ms on Freed Desktop and 800 ms on a conservative supported iPhone.
- First interactive frame under 700 ms on Freed Desktop and 1,200 ms on iPhone.
- Gesture p95 under 16.7 ms on Freed Desktop and 24 ms on iPhone.
- Main-thread gesture work under 3 ms with no React state writes.
- No tasks over 50 ms during pan or pinch.
- Target fewer than 12 steady-state GPU draw calls.
- A 10x increase in feed items must not materially change entry time because graph cost follows summary size.
- Validate installed Freed Desktop and real iPhone Safari, not only headless Chromium.

## Approved Engineering Roadmap

1. Capture the current installed behavior, screenshots, GPU limits, and diagnostic baseline when the machine is available for trustworthy measurement.
2. Build a standalone renderer laboratory with 30,000 semantic nodes and 100,000 background stars. Compare raw WebGPU, Three WebGPU, WebGL2 fallback, and a small Metal proof only if warranted.
3. Replace mount-time item scans with an incrementally maintained graph index and transferable typed arrays.
4. Build the deterministic hierarchical galaxy compiler with semantic zones, local identity systems, bounded prominence depth, stable coordinates, and incremental patches.
5. Build the GPU engine with instanced stars, procedural coronae, short links, provider fields, batched billboard text, texture-atlas avatars, and depth-aware picking.
6. Build the locked camera and native touch plus pointer gesture state machine with transform-only movement.
7. Restore every workflow: selection, details, context menus, linking, pinning, promotion, suggestions, reconnect state, reader links, and Map links.
8. Add the development visual laboratory for nebula, rings, combined fields, plane thickness, prominence curves, and label density.
9. Prove stress behavior, mobile interaction, accessibility, reduced motion, theme coverage, installed diagnostics, and memory budgets.
10. Cut over only after the new engine beats the current graph on usefulness and measured performance, then remove the old renderer.

## Approved Decisions

- Galactic plane with a locked perspective camera.
- Prominence depth dominated by relationship and care, with bounded activity and reconnect boosts.
- Stable provider sectors around the outer rim.
- Reconnect as a transient aurora or filter rather than permanent spatial relocation.
- WebGPU-first shared engine, WebGL2 fallback, Metal behind a benchmark gate.
- All semantic stars remain GPU-resident during movement.
- Full-width visual render layer plus a separate bounded interaction lane.

## Benchmarking Note

As of July 12, 2026, other tasks are running on the development machine. The owner has authorized benchmarking only when a case is repeated enough to establish a consistency range. Single-run timing results are not decision evidence. Correctness checks, builds, deterministic unit tests, and visual architecture work may continue independently.

## Implementation Progress

### Foundation complete

- The current atlas compiles into a versioned renderer-neutral scene protocol backed by typed arrays.
- Semantic depth is care-dominant, with bounded activity and recency contributions.
- Linked accounts remain behind their identity while preserving stable deterministic positions.
- The atlas worker transfers scene buffers instead of making the main thread compile duplicate GPU input.
- Hover and selection update flags, emphasis, and point sizes in place without replacing static positions or indexed edges.
- An imperative `IdentityGalaxyEngine` owns Three.js, the batched glyph label layer, palette resolution, WebGL fallback selection, resize, synchronization, rendering, and disposal.
- `FriendGraph.tsx` now owns React props, worker lifecycle, interaction orchestration, and product callbacks rather than renderer internals.
- Semantic model construction and viewport atlas slicing are separate operations.
- The worker caches the full rich semantic model by source revision.
- The first response for a source revision transfers every semantic star. Later viewport responses retain those node buffers and transfer only capped atlas metadata plus edge indices.
- Resident semantic stars are now distinct from visible interaction detail in diagnostics and regression coverage.

### First native galaxy pass complete

- The graph now uses a locked perspective camera over the galactic plane. Pan and zoom move the camera while preserving the established plane transform and bounded prominence depth.
- Every semantic star is rendered through one instanced WebGL2 quad mesh with procedural cores, coronae, rays, and selection rings. No retained scene object exists per star.
- Settled relationship spokes use one instanced edge mesh with screen-stable width. Gesture quality keeps its stricter edge and interaction caps without evicting semantic stars.
- Projected picking uses the active camera, depth-scaled hit radii, and capped worker candidates.
- Confirmed friends use a deterministic golden-angle disk sized by local system footprint. Sparse linked accounts occupy complete local orbits instead of partial arcs.
- Model compilation builds one account-to-person index and never rescans the full account library for each person.
- The initial camera opens at the first useful semantic tier on Freed Desktop and at a closer exploration tier on iPhone. `Fit all` still reveals the complete provider universe.
- Billboard labels use one instanced glyph draw backed by a theme-font canvas atlas. A bounded candidate pool stays resident while current-camera projection, zoom-density admission, and screen-space collision run on every changed frame.
- iPhone pan and pinch use native non-passive touch events rather than WebKit pointer capture. Pinch preserves its active midpoint, hands directly to one-finger pan when either touch lifts, recovers after touch cancellation, and leaves browser zoom unchanged. Mouse and pen input remain on Pointer Events.
- Mac Safari trackpad pinch uses native `gesturestart`, `gesturechange`, and `gestureend` events. Cumulative Safari scale is converted into an incremental ratio on every event, so direction reversal responds immediately through the same midpoint-preserving camera transform used by touch and wheel input. Trackpad inward input uses the ordinary multiplicative ratio on its first event, even inside the compressed overview band. Safari gesture release and Chromium control-wheel pinch release continue through a bounded logarithmic momentum controller around the last focal point.
- Active pan and pinch issue camera renders plus bounded, coalesced presentation requests. They do not synchronize semantic scene buffers, rebuild edges, rerasterize label text per frame, load avatars, or write React state on movement. A changed bounded worker candidate set may replace the label texture without touching semantic geometry.
- The animation frame ID is the sole draw scheduler latch. Effect cleanup cancels and clears that ID so React development verification cannot leave gesture rendering permanently blocked.
- The canvas compatibility renderer caches its decorative star background, avoids per-node blur filters, keeps connectors visible, and draws a capped screen-space label set during motion.
- Mobile regression coverage now includes browser-generated two-finger input in the PWA, pinch-to-pan continuation, and the same touch state path under WebKit's iPhone profile. Linking and pinning remain context-menu-only workflows.
- Neon and Scriptorium visual passes confirm that node cores, background stars, labels, edges, and provider fields follow the active theme.
- Provider sectors now use deterministic logarithmic spiral placement shared by the atlas and renderer. Procedural nebula arms, themed dust, and constellation streams replace simple elliptical region outlines.
- Linked accounts occupy tighter local identity systems, with their person as the stable center and short semantic links available only while that constellation is hovered or selected.
- Billboard labels anchor directly to their parent stars, use a stronger outline, and continuously refresh the accepted density-capped roster during camera movement.
- Graph utility controls use the shared Freed secondary button surface so their backgrounds remain legible in every active theme.

## Finalization Audit

The shared renderer foundation now has more than 240 deterministic tests and the product route has replaced its former direct `IdentityGalaxyEngine` ownership, whole-atlas React orchestration, and `runAtlasOnMainThread` fallback. The retained WebGL2 compatibility backend may still wrap that low-level renderer behind the shared backend contract. The same source, viewport-presentation, sparse-activity, direct-interaction, navigation, input, renderer-host, and recovery modules are now consumed by both the laboratory and the shipping Friends surface. The audit table records the evidence available before cutover, the integrated acceptance completed before release, and the corrections made from installed production evidence.

| Area | Detached evidence | Remaining production requirement |
|---|---|---|
| GPU backend | Raw WebGPU is selected, Three.js WebGPU remains a reference, and WebGL2 is the deterministic compatibility backend. All three implement one shared generic renderer contract for IDs, detail tiers, transform, metrics, state updates, bounded presentation, picking, rendering, health, and disposal. They also consume one shared renderer scene envelope containing the atlas, typed scene, sparse interaction index, direct-upload stars, decorative buffers, and source counts. Synthetic fixture diagnostics stay outside that envelope. The shared backend runtime stages hidden candidates while the current canvas stays visible, commits by generation, disposes stale surfaces, retains healthy compatibility state, recovers once, and latches terminal compatibility failure. The shared direct-upload contract defines the eight-float star instances, palette roles, one motion-invariant semantic quad, and a decorative motion cap consumed by the worker and raw backend. One shared complete renderer palette owns surface, text, semantic, selection, and provider colors. Its typed-scene resolver supplies Three.js buffers and avatar seeds without fixture helpers, while Raw WebGPU resolves normalized theme colors, light-surface policy, and its fixed uniform block. | Keep exact star appearance and live bounded labels in parity across Raw WebGPU, Three.js WebGPU, and WebGL2 as renderer work evolves. |
| Semantic stars | All 30,000 typed semantic stars remain GPU-resident during pan, pinch, selection, theme changes, motion-density changes, and backend recovery. | Feed the resident scene from the real identity worker and preserve every product selection and details callback. |
| Background stars | Raw WebGPU retains 100,000 decorative stars and selects a deterministic 50,000-star motion prefix without uploads or scene rebuilds. | Preserve the same resident and motion policy after product cutover. |
| Interaction updates | The shared worker-transferable index owns stable-ID lookup, compact adjacency, and sparse depth-aware picking. The shared allocation-bounded scene state reuses one role map and contextual-edge buffer. Hover and selection then use one fixed-capacity overlay upload, one bounded contextual-edge upload, and retained render bundles. | Route real product selection, context-menu linking, context-menu pinning, and details state through these exact shared modules without React movement state. |
| Provider fields | Six retained procedural fields use shared deterministic geometry and presentation contracts, active-theme colors, Nebula by default, a bounded motion shader, and close-detail culling. Geometry compiles once, while theme and style changes rewrite only the fixed presentation block. | Remove development controls from production while preserving provider geography and every active theme. |
| Labels | One instanced billboard glyph batch remains resident during movement. Text measurement, rasterization, packing, and UV assignment occur only when the bounded worker candidate source changes. Every changed camera frame performs allocation-bounded perspective projection, zoom-density admission, and screen-space collision against that resident pool, then uploads only the small instance stream when the accepted roster changes. Product scenes restrict label and avatar candidates to worker-admitted atlas metadata, so the main thread never scans all resident people for presentation. | Preserve continuous roster updates, bounded candidate counts, and fallback parity in product acceptance. |
| Avatars | A hidden startup atlas admits at most 12 desktop or 6 compact identities from bounded worker metadata. Close settle can replace it with the perspective-visible roster. Raw WebGPU and Three.js WebGPU retain the atlas while one elapsed-time controller fades a scale-derived opacity uniform, with no candidate scan, atlas build, image decode, label rebuild, or star write during zoom. The shared keyed admission state and image queue deduplicate source revisions, bound decoding, reuse results, reject stale completions, cache failures, evict least-recently-used entries, and dispose owned bitmaps. The shared compositor owns image clipping, initials fallback, selected borders, packing, UVs, and the stable instance stream. | Connect real product avatar candidates to the shared admission and compositor modules and prove selected identity priority in Freed Desktop and the PWA. |
| Camera and input | One shared imperative navigation controller owns the locked transform, full-background insets, fitted and initial framing, near and far clip-safe limits, stable-ID depth focus, pan, directional fixed-focal zoom, moving-focal Safari zoom, midpoint pinch, resize reanchoring, and source replacement. The detached lab and product engine both consume it. Native iPhone touch, Mac trackpad pan and pinch, pointer, touch, and trackpad zoom inertia, reduced-motion cancellation, keyboard navigation, one accessible graph region, and stable details plus context-action requests remain layered around that controller. | Move the existing event registration into the reduced product shell and complete the maintained menu-focus and search-to-focus workflows without rebuilding camera state in React. |
| Presentation lifecycle | One explicit switch suspends frames, settle, labels, avatars, hover, long press, contacts, and inertia while retaining the exact canvas, backend, camera, selection, and GPU scene. The graph region becomes inert and leaves the accessibility tree until resume. | Bind the switch to the existing mobile Details lens and prove product focus restoration without mounting a second graph. |
| Shared runtime contracts | Full-canvas geometry, bounded context anchors, galactic-plane pin coordinates, details requests, keyboard commands, long press, the imperative `fitAll`, `focusNode`, and presentation handle, fixed pointer roster, bounded inertia, scalar settle scheduler, presentation-aware demand-frame predicate, clip-safe camera, depth-correct focus, directional zoom, wheel normalization, midpoint-preserving pinch, fixed diagnostic sample ring, passive long-task monitor, first-failure backend health latch, the generic renderer interface, shared renderer scene envelope, complete renderer palette plus typed-scene semantic resolver, motion render-density policy, atomic generation-safe backend activation and recovery, keyed avatar admission state, bounded avatar image queue, transferable stable-ID and sparse-pick index, allocation-bounded interaction scene state, allocation-free world-to-screen projection, theme-aware label plus avatar billboard compositors, direct-upload star instance plus geometry contracts, fixed Raw WebGPU color block, provider-field geometry plus presentation contracts, source-key incremental activity indexing, and sparse typed activity patch encoding now live under shared UI source and are consumed directly by the laboratory. | Make the reduced React shell delegate its existing ref, callbacks, input lifecycle, camera, gestures, diagnostics, backend factories, recovery, avatars, stable-ID lookup, sparse interaction lifecycle, presentation admission, overlay projection, billboard construction, palette, provider fields, activity revisions, and worker-to-GPU streams to these exact modules, with no translated interaction or observability layer. |
| Graph index | The shared activity index retains bounded source summaries and the shared encoder produces proportional typed scene patches. The laboratory and detached product worker apply them as sparse raw WebGPU writes without item payloads or position changes. The renderer host journals only the latest changed node state for backend recovery. | Connect document-worker feed deltas to the shared index, feed their existing key patches into the completed activity lane, then delete whole-item Friends mount derivation. |
| Source revisions | Worker-only startup transfers 21 numeric buffers, caps rich metadata at 192 nodes, and never includes viewport movement in semantic compilation. The detached product worker separates deterministic source, viewport-presentation, and sparse-activity revisions. It caches the real atlas model and source bindings, transfers the resident scene once, returns later bounded metadata with no semantic transfer, and encodes activity changes into six typed buffers. Its browser client independently bounds and coalesces presentation and activity work, admits replies before callbacks, isolates generations, and uses the shared health poll for lane-specific scalar deadlines with no browser gesture timer. Hover and selection bypass the worker and update the resident stable-ID interaction index directly. | Keep document-worker summary ownership as the remaining upstream Phase 8 task. |
| Stress proof | The detached worker fixture contains 5,000 people, 25,000 accounts, 25,000 activity summaries representing 250,000 items, 30,000 semantic stars, and 100,000 decorative stars. Represented item volume adds no item payload or transfer bytes. | Feed the same contract from real document-worker summaries and prove integrated graph entry follows summary size rather than item count. |
| Diagnostics | The laboratory reports renderer, fallback, source scale, star counts, presentation counts, contextual edges, sparse picks, uploads, render density, frame-loop state, camera safety, and passive long-task evidence or explicit unsupported state. One click exports the bounded identity-free schema. | Route the same schema and passive observer through the product renderer heartbeat and preserve one-click installed export. |
| Device proof | Desktop and compact browser probes confirm nonblank output, touch and trackpad semantics, theme parity, labels during movement, backend recovery, and far-clip protection. | Record repeated consistency ranges on installed Freed Desktop and a physical iPhone using the exact integrated build. |

### Finalization Slices

1. Complete: backend-neutral diagnostics, retained GPU resources, sparse interaction writes, worker-only compilation, Raw WebGPU selection, WebGL2 recovery, batched labels, bounded avatars, native touch, trackpad navigation, inertia, theme parity, camera safety, and resident presentation suspension.
2. Complete: reconcile current `origin/dev`, move the selected renderer and proven primitives into shared product modules, and make the laboratory consume the same modules.
3. Complete: replace the large `FriendGraph` internals with the reduced lifecycle shell while preserving its ref, callbacks, context menu, pin, link, promotion, Details, identity-mode, theme, and diagnostics contracts.
4. Complete for the graph surface: connect source, viewport-presentation, sparse-activity, and direct-interaction lanes, then delete the main-thread compiler fallback. Document-worker ownership of the upstream summary index remains separate Phase 8 work.
5. Complete: run the maintained Friends acceptance matrix, accessibility workflows, backend recovery, PWA touch paths, active-theme visuals, production Desktop build, and diagnostic export on the integrated feature branch.
6. Deferred by evidence policy: record repeated installed Freed Desktop and physical iPhone consistency ranges only when the machine is suitable for meaningful comparison.
7. Complete for the initial release: merge through `dev` and ship the integrated Galaxy in production v26.7.2300. The current correction restores live labels, stable semantic-star intensity, and shared sidebar geometry after installed acceptance exposed those regressions.

### Next structural target

After the motion-presentation correction ships, move the proven incremental activity index into the shared document-worker path so overview and detail data also stop depending on Friends-view item scans. Raw WebGPU remains preferred, WebGL2 remains the deterministic compatibility backend, and native Metal remains unnecessary unless later device evidence changes that decision.

### Product cutover boundary

The product integration is an internal graph-surface replacement. It is not a rewrite of Friends workflows.

- Preserve the public `FriendGraph` prop and ref contract used by `FriendsView`. Selection, details, relationship promotion, account linking, device-local person pins, device-local account pins, identity mode, and mobile Details mode remain owned by the existing product shell.
- Bind mobile Details mode to the engine presentation lifecycle. Details hides and suspends the resident graph without unmounting its canvas or disposing GPU resources. Returning to the graph resumes the same backend, camera, selection, and semantic scene.
- Preserve context-menu-only pinning. A pointer or long press opens the node menu. `Pin here` persists the camera-projected world point through the existing person or account callback. No node drag or drag-to-pin path returns.
- Preserve context-menu-only linking. An account menu opens the existing searchable person picker and calls the existing account-to-person callback. No drag-to-link target or hidden gesture returns.
- Preserve `focusNode(id)` for reader author links, search results, Map handoff, selected details, and programmatic navigation.
- Keep the context menu and searchable picker in the React overlay layer. Move them to the shared bounded menu and standard control vocabulary during cutover. They must remain inside the viewport and usable by keyboard, pointer, and touch.
- Replace the 1,920-line `FriendGraph.tsx` internals with a small lifecycle and product-callback shell. The imperative engine owns the canvas, backend selection, camera, render loop, resize, GPU resources, picking, interaction buffers, palette, detail tier, and recovery.
- Move the selected raw WebGPU backend from the laboratory into one shared product module. The laboratory imports that module afterward. Do not fork a second raw renderer.
- Reuse the proven pointer roster, settle scheduler, demand-driven frame loop, density policy, sparse scene index, render bundles, label atlas, avatar admission, and backend-health primitives. Do not translate them back into React state, Maps, per-event objects, or browser timers.
- Delete `runAtlasOnMainThread` and every timeout path that compiles the complete semantic graph in React. A worker startup failure either keeps the last-good scene or enters the bounded WebGL2 compatibility state with a visible diagnostic reason.
- Connect the completed source, viewport-presentation, sparse-activity, and direct interaction lanes. Camera movement may send only bounded, coalesced presentation requests. Resize changes presentation dimensions without rebuilding the semantic source.
- Keep all semantic stars resident in raw WebGPU. Motion may use the proven decorative prefix, reduced density, coarse field shader, and close-field cull. Semantic stars must retain the same geometry, luminous profile, and intensity at rest and in motion. Motion may not evict identities, accounts, labels, avatars, contextual edges, or interaction state.
- Retain WebGL2 and canvas as deterministic compatibility paths. Recovery preserves theme, camera transform, selected node, identity mode, and available detail metadata.
- Keep Friends full bleed beneath the shared sidebar exactly like Map. Fit and focus calculations use the visible workspace inset while rendering continues beneath the sidebar.
- Route graph diagnostics through the renderer heartbeat and one-click export. Installed Freed Desktop and PWA builds must report backend, fallback reason, resident and motion counts, labels, avatars, contextual edges, sparse pick candidates, buffer uploads, render density, long tasks, and frame-loop state.
- Remove the development backend and field controls from production output. Keep them in feature previews until raw WebGPU, WebGL2 recovery, Nebula, and active themes pass user acceptance.
- Do not publish the cutover from a stale base. Reconcile current `origin/dev` only after global behavioral custody is explicitly released, then rerun focused graph tests before adapting any changed product boundary.

The integration sequence is deliberately narrow:

1. Move renderer-neutral laboratory primitives into the product library without changing the active route.
2. Add the raw backend to `IdentityGalaxyEngine` behind an explicit backend contract and retain the current engine as fallback.
3. Replace `FriendGraph` scene and input orchestration while preserving its external props, ref, context menu, controls, and diagnostics hooks.
4. Delete the main-thread atlas fallback and superseded gesture code in the same change that proves worker failure recovery.
5. Verify linking, pinning, promotion, selection, focus, fit, theme, mobile Details mode, full-bleed layout, and fallback before any performance verdict.
6. Run repeated consistency baselines only after the machine workload permits comparable runs, then perform physical iPhone and installed Freed Desktop acceptance.

### Full-canvas topology

Friends must follow Map's two-lane canvas pattern instead of extending the current interactive graph element underneath the primary sidebar.

- `AppShell` owns a `friends-background-layer` beside the existing `map-background-layer`. It spans the complete content frame below the header and sits behind the primary sidebar, Friends workspace, detail rail, and debug drawer.
- `FriendGraph` portals only the GPU canvas into that background layer. The canvas has `pointer-events: none`. No menu, control, focus target, or gesture listener lives in the full-bleed layer.
- The existing `friend-graph-viewport` remains inside the Friends workspace. It becomes the bounded focusable interaction overlay and owns pointer, touch, wheel, keyboard, context-menu, and accessibility behavior.
- The engine receives both rectangles. Canvas coordinates equal interaction coordinates plus the measured interaction-to-canvas offset. Pin world coordinates, picking, midpoint-preserving zoom, and focus calculations use that conversion.
- The usable camera inset is derived from the canvas and interaction rectangles. Its left edge excludes the primary sidebar. Its right edge excludes the open Friends detail rail and any debug drawer. Fit and focus center the semantic target inside this usable rectangle while the starfield continues beneath the surrounding chrome.
- One coalesced `ResizeObserver` updates the canvas dimensions and usable inset only when shell geometry changes. Camera movement never measures DOM geometry and never writes React state.
- The context menu, searchable person picker, development variation control, Fit all, diagnostics control, collapsed detail card, and mobile Details lens stay in the bounded React layer. They never become GPU content.
- When mobile switches to the separate Details lens, the engine keeps its resident scene and camera but stops drawing until the graph lens is visible again. Returning to Graph restores the same selection and camera without recompiling the scene.

This topology is the required fix for the PR 887 failure mode. Visual bleed and pointer ownership are separate by construction, so empty-space clicks, sidebar controls, and graph controls cannot compete for the same DOM surface.

### Accessibility contract

- The full-bleed GPU canvas remains `aria-hidden` and never creates one DOM node per star.
- The bounded interaction lane is one focusable graph region with a visible theme-aware focus ring. Its accessible description identifies the active identity mode, selected identity or channel, and available camera controls.
- Arrow keys pan. Plus and minus zoom around the usable workspace center. Home and zero fit the galaxy. Escape clears selection or closes the active graph menu. Enter opens details for the selected node. Shift+F10 and the Context Menu key open the selected node menu.
- Search remains the primary non-spatial identity navigator. Choosing a result calls the same `focusNode(id)` path used by Reader and Map, moves the camera, selects the canonical node, and announces the result.
- Selection, focus completion, backend recovery, and unavailable actions publish concise updates through one polite live region. Camera movement does not announce every frame or every star crossed by the pointer.
- The context menu uses shared bounded menu behavior. It moves focus to its first enabled command, supports normal arrow and Tab navigation, keeps the searchable person picker inside the viewport, and restores focus to the graph region when closed.
- Mobile long press opens the same menu without enabling node drag. A second touch or movement beyond the tap threshold cancels the pending long press.
- Reduced motion disables twinkle, pulse, inertial pan, inertial zoom, and animated focus travel. It does not remove labels, depth ordering, selection emphasis, reconnect state, or provider geography.
- Details, overview lists, search results, and suggestions remain ordinary semantic HTML. The canvas is an exploration surface, not the only way to inspect or change identity data.

### Product acceptance matrix

The cutover should preserve existing test IDs where they still describe the product contract. Tests that assert obsolete renderer internals must be adapted, not deleted without replacement.

| Contract | Existing maintained proof | Cutover requirement |
|---|---|---|
| Workspace navigation | `smoke.spec.ts`: Friends return navigation and workspace back navigation | Keep the same app route, header, sidebar, and back behavior |
| Desktop detail rail | `smoke.spec.ts`: rail visibility persistence and 400-pixel resize cap | Keep selection, persisted open state, resize handle, width cap, and collapsed detail card |
| Mobile lens | `smoke.spec.ts`: mobile toolbar switches between Graph and Details | Suspend drawing in Details, then restore the exact camera and selection in Graph |
| Selection lifecycle | `smoke.spec.ts`: pending-atlas selection, collapsed card, and empty-space clear | Preserve selected person and account callbacks across worker startup and backend recovery |
| Canonical graph content | `smoke.spec.ts`: confirmed friends, provisional people, and channels render together | Keep Person plus Account identity systems, Friends and All Content modes, and provider sectors |
| Explicit relationship changes | `smoke.spec.ts`: suggestion promotion, linked-account promotion, and relationship slider | Keep Followed, Friends, and Fam actions explicit. Rename internal drop terminology during cutover without restoring node drag |
| Suggestions | `smoke.spec.ts`: ranked suggestion promote and dismiss | Keep local ranking, explanation, confirmation, and non-destructive dismissal |
| Context linking | `smoke.spec.ts`: channel context-menu link survives reload | Keep account context menu, searchable person picker, canonical document mutation, and reload proof |
| Device-local pinning | `smoke.spec.ts`: person context-menu pin survives reload without Automerge fields | Keep context-menu-only pinning and the device layout store. Pan must never create pins |
| Labels during movement | `smoke.spec.ts`: zoom labels remain visible, the layout counter advances, the roster stays density-capped, and stress pan causes no semantic scene rebuild | Keep the bounded label texture resident, then reproject and collision-admit the current roster on every changed camera frame |
| Touch camera | `smoke.spec.ts`: two-touch midpoint pinch and pinch-to-pan continuation | Route native touch, Safari gesture events, wheel, and pointer pan through one locked-camera transform |
| Reader handoff | `reader-hydration.spec.ts`: author link opens channel details in Friends | Preserve `focusNode(id)` and canonical account selection across lazy engine startup |
| Map handoff | Map and Friends smoke flows for marker linking, promotion, and post navigation | Preserve shared identity mode and canonical person or account focus |
| Themes and controls | `theme-switching.spec.ts`: Friends control placement and opaque control backgrounds | Replace the old bounded-canvas geometry assertion with full background coverage plus bounded interaction and control-lane assertions |
| Keyboard and assistive access | The detached lab proves one focusable described region, camera shortcuts, Enter details, Shift plus F10 actions, cancellable mobile long press, selection and recovery announcements, reduced-motion description, hidden canvases, and no per-star DOM | Add maintained product coverage for rendered menu focus restoration, identity-mode description, and search-to-focus |
| Dense visual structure | `smoke.spec.ts`: maintained Scriptorium dense snapshot | Update snapshots only after Raw WebGPU is accepted in Scriptorium and one dark theme |
| Renderer diagnostics | `perf-friends.spec.ts` and renderer heartbeat assertions | Replace dense-scene internals with backend, fallback reason, resident counts, sparse writes, presentation counts, render density, and frame-loop state |
| Representative scale | Current perf fixture uses 1,600 people and 1,920 accounts | Add 5,000 people, 25,000 accounts, and a 250,000-item summary-equivalent source before performance acceptance |
| Recovery parity | No complete maintained Friends backend-loss workflow yet | Add Raw WebGPU loss recovery that preserves theme, camera, selection, detail tier, and functional WebGL2 or canvas controls |
| Mobile context actions | Desktop context-menu workflows plus current long-press implementation | Add maintained touch long-press coverage for Open details, Pin here, Link to person, and explicit relationship actions |

Benchmarking is authorized only as repeated consistency runs while other machine tasks remain active. Single-run timing results are not decision evidence.

## Renderer Laboratory Checkpoint

The July 17, 2026 runtime-neutral renderer laboratory lives under `packages/ui/labs/friends-galaxy`. It is intentionally detached from PWA and Freed Desktop entry points while the global behavioral product slot remains occupied by stability work.

The laboratory builds one deterministic renderer-neutral fixture and feeds the identical typed positions, sizes, prominence depth, provider assignments, and theme roles to three backends:

| Backend | Semantic field | Decorative field | Submission shape |
|---|---:|---:|---|
| Current production Three.js WebGL2 | 30,000 stars | Existing viewport cap of 7,000 or 12,000 stars | Production engine, draw-call count not yet exposed |
| Three.js WebGPU | 30,000 stars | 100,000 stars | Four overview draws, five while contextual links are active |
| Raw WebGPU and WGSL | 30,000 stars | 100,000 stars | Four overview draws, five while contextual links are active |

The fixture represents 5,000 identities and 25,000 attached or unlinked channels. Its 25,000 compact source summaries represent 250,000 feed items without allocating or transferring those items. Summary item counts contribute bounded channel activity signals while the worker output remains proportional to people, channels, summaries, and decorative stars. Identity systems occupy a deterministic six-arm galactic plane. Linked channels remain in short local orbits. Unlinked channels occupy five provider spiral sectors. Each provider uses its stable seed and three-to-five-arm geometry, with bounded angular, radial, and lane-width scatter so the sectors remain organized without looking stamped from one template. Prominence remains bounded on the z axis so camera pan creates restrained parallax without random spatial scattering.

The laboratory and future product shell now share one renderer palette map keyed by Freed's actual Scriptorium, Neon, Midas, and Ember theme ids. The former laboratory-only Vesper name is gone. Light themes reduce background opacity and avoid additive bleaching. Dark themes retain brighter coronae, while each palette keeps provider identities distinct instead of collapsing into one hue family. The raw WebGPU path preserves the Scriptorium palette more faithfully than the Three.js WebGPU path. The control panel uses an opaque theme surface because blurred translucent overlays produced WebGPU compositor artifacts in browser verification.

The raw WebGPU path adds one procedural field batch containing exactly six instances: one identity-core field and one field for each provider sector. The shared field compiler scans bounded person positions and atlas regions once to produce the fixed twelve-float geometry stream. A separate shared presentation writer applies active-theme color, opacity, and style codes in place. Theme or variation changes therefore rewrite only the six-instance field buffer without rescanning people or replacing its storage. Empty identity scenes remain finite, and unknown provider roles use the account color. The WGSL fragment path uses domain-warped noise and an irregular soft envelope rather than a flat ellipse. Nebula, Star streams, and Cosmic blend remain available through the development control, with Nebula selected by default. Star streams use broken filaments that fade near the core, and every field fades as the camera approaches so close-detail stars, labels, and avatars stay legible. Camera movement changes the shared view transform and never rebuilds field geometry.

Once camera scale reaches 1.5, the field vertex path emits one degenerate off-screen position for every field corner. The existing detail fade has already reduced the nebulae to near-invisible atmosphere at that scale. This prevents the six large field quads from rasterizing or invoking noise fragments in deep close detail while preserving their complete overview and middle-detail appearance.

The raw backend pre-records four WebGPU world bundles over shared resources: settled and moving variants, each with or without the fixed-capacity interaction overlay. Settled bundles draw the procedural fields, all 100,000 decorative stars, and all 30,000 semantic stars. Moving bundles keep every semantic star and draw a deterministic 50,000-star decorative prefix whose index-keyed placement remains spatially representative. Contextual edges, avatar billboards, and label billboards each have a retained presentation bundle when active. Each camera frame writes the shared transform uniform, points one reused render-pass attachment at the current canvas texture, executes one ordered frame-bundle list, and submits through a reused command-buffer container. This removes repeated pipeline, bind-group, vertex-buffer, and draw setup from the JavaScript gesture path without freezing buffer contents. Theme, field-style, sparse activity, and sparse interaction writes remain visible through the pre-recorded commands. Interaction and settle boundaries select or replace only the affected bundle. One activity update rewrites only the affected 32-byte base semantic instance and refreshes the bounded overlay once if that identity system is active.

Raw WebGPU diagnostics include an exact tracked payload count for resident vertex, uniform, edge, label-atlas, avatar-atlas, and procedural-field data. The count deliberately excludes opaque driver, pipeline, bind-group, swap-chain, and command-buffer overhead, so it remains deterministic and does not pretend to be process memory. The laboratory formats it as locale-aware bytes, KiB, or MiB.

The laboratory now exposes one bounded diagnostic export shared by its visible control and inspection API. Schema version 1 contains source and summary counts, renderer and adapter identity, fallback reason, resident and motion counts, draw and bundle shape, presentation counts, upload counters, activity patches, camera limits, viewport dimensions, input mode, recovery state, fixed-ring frame summaries, and passive long-task evidence. It deliberately contains no node IDs, person IDs, account IDs, names, avatar URLs, sample item IDs, or feed payloads. Missing and non-finite backend values normalize to `null`, and renderer error text is length bounded. A `PerformanceObserver` records only count, total duration, worst duration, and latest start time when the browser advertises the Long Tasks API. Unsupported browsers report an explicit unsupported shape with nullable counters. The monitor installs no timer, performs no work in camera handlers, and disconnects during renderer teardown. The fixed sample ring, passive long-task monitor, and first-failure backend health latch now live in shared UI source and are consumed by the detached shell and both WebGPU backends. Clipboard rejection remains local, restores the control, and reports failure through the existing status surface.

The shared WebGPU shell caps settled Retina rendering at 1.5 device pixels per CSS pixel. Active camera motion temporarily uses 1.0 on compact canvases and 1.25 on wider canvases, then restores settled density after 140 ms. Input handlers mark one pending density transition and request a frame instead of resizing the GPU canvas synchronously. The render callback coalesces repeated input and applies the latest density once before drawing. The settle callback restores settled density through the same path. These transitions do not evict stars, rebuild labels, decode avatars, or change semantic detail. Both WebGPU backends report the active render density. This policy follows the existing production engine cap while reducing compact motion fill to less than half of an uncapped 3x iPhone surface.

Raw WebGPU treats animation and motion as uniform state rather than semantic scene state. Semantic stars use the same four-vertex quad, complete luminous fragment profile, and fixed intensity during movement and rest. Semantic twinkle is disabled, so starting a drag cannot make identities flare or gain visual weight. Optional ambient twinkle remains limited to decorative background stars. Camera motion still selects a prepared world bundle without rebuilding geometry or command state, uses the deterministic decorative prefix, and exits the procedural field shader after one coarse noise sample, one irregular envelope, and, for stream variants only, one spiral phase. Settle restores all decorative stars and the complete provider-field shader. The existing camera-scale float carries motion through its sign, while its absolute value preserves camera-detail fading. This changes no uniform-block size, bind group, GPU instance buffer, resident-star count, or semantic draw topology at runtime.

Diagnostic frame and submission samples now live in fixed 240-value `Float64Array` rings. Gesture frames overwrite one slot instead of shifting JavaScript arrays. The diagnostics panel does not rebuild its DOM during active camera motion. It refreshes after settle from one bounded ring snapshot, so instrumentation cannot inject periodic layout, allocation, or array-copy work into pan and pinch.

Raw and Three WebGPU diagnostics expose independent label-atlas and avatar-atlas build counters. Raw WebGPU additionally reports current visible labels and live label-layout passes. Camera movement advances layout without rebuilding the text atlas, semantic scene, edges, or avatars. A worker candidate change may replace the bounded label atlas, while settle may admit one new avatar roster. These counters make continuous presentation and semantic-scene residency directly inspectable without treating aggregate buffer uploads as a proxy.

The renderer loop is now demand-driven when animation is disabled. Dirty GPU state and pending settle work request frames, then the loop stops once both are clear. Changed diagnostics wake through the bounded refresh interval instead of keeping a permanent animation callback alive. One persistent 250 ms health poll consumes the backend's one-shot fatal signal and requests recovery or a diagnostic frame only when needed. The viewport exposes active versus idle frame-loop state for structural browser checks. Explicit animation still keeps the normal continuous frame loop.

Settled detail no longer cancels and recreates a browser timeout on every pinch or wheel event. Movement writes one generation and one deadline into a persistent scheduler. The existing animation loop consumes the latest generation once the 140 ms boundary passes, restores settled density, updates the detail tier, and starts bounded avatar admission. Gesture events create no settle closure or timer.

Interaction uses the production locked-camera transform. Native touch pan and pinch, pointer pan for mouse and pen, Mac two-finger wheel pan, control-modified wheel pinch, and Safari gesture zoom all update the transform directly. Touch-capable hardware registers non-passive `touchstart`, `touchmove`, `touchend`, and `touchcancel` handlers and suppresses the duplicate touch Pointer Event path. One cached viewport origin is shared across every input path and refreshed only at interaction entry, gesture start, the first wheel event in a burst, and resize. Active touch state lives in one fixed-capacity typed roster with ordered pointer IDs, current positions, and gesture origins. Pan and pinch perform indexed scalar reads and writes without creating a Map iterator or point object. Removing either touch shifts the survivor in place and cancels the former pinch settle deadline, so one-finger continuation cannot trigger a presentation rebuild underneath the remaining contact. Touch cancellation clears the roster before scheduling settle, so a canceled WebKit sequence cannot strand stale contacts. Hover coalescing stores only the latest scalar pair. Safari trackpad zoom stores its viewport origin, previous midpoint, and previous cumulative event scale as scalars. Each `gesturechange` converts that cumulative value into an incremental ratio before applying the directional curve. Reversing inward therefore responds on the first event, even when Safari still reports a cumulative scale below one for the complete gesture. Inside the compressed overview band, the first inward ratio uses ordinary multiplicative zoom without replaying the outward resistance curve in reverse. Keyboard zoom and native two-touch pinch continue to use their exact ratios. Trackpad pinch samples only scalar log-scale velocity and the latest focal coordinates. The handler performs no point-object allocation or viewport geometry read during movement. The laboratory exposes its selected touch and wheel input modes, inertial state, and viewport geometry-read count in visible diagnostics and inspection state. Pure gesture tests confirm the exact touch-distance scale ratio, moving-midpoint preservation, scale clamping, centered zoom anchoring, immediate Safari reversal, wheel-delta normalization, typed-roster ordering, cancellation reset, touch handoff, zoom momentum direction, and outer-target safety. A 390 by 844 browser probe confirmed that a synthetic two-touch expansion changed camera scale by the exact touch-distance ratio while preserving the world point beneath the midpoint.

Pointer and one-finger touch release can continue through one bounded inertial pan controller. Event samples retain only scalar velocity. The demand-driven frame loop consumes one reused result object, applies exponential decay with a capped launch speed, and avoids per-frame square root work unless an input sample exceeds the cap. Mac trackpad pan does not enter the custom controller because macOS already emits its own momentum wheel sequence. Trackpad pinch uses a separate bounded controller in natural-log scale space. Safari starts it on `gestureend`. Chromium control-wheel input advances one 72 millisecond scalar release deadline, so a pinch burst starts momentum without allocating or canceling browser timers on every event. The zoom controller decays around the latest focal point, uses the existing directional outer envelope, and stops when that envelope absorbs visible movement. Both controllers cancel on new input, resize, backend change, visibility loss, excessive frame delay, or reduced-motion preference. A stopped throw schedules the existing scalar settle deadline once. Existing functional desktop checks prove ordinary wheel input changes only camera position, control-modified wheel input changes scale, a drag continues after release, and a tap stops and settles active pan momentum. Deterministic zoom checks prove inward and outward momentum direction, bounded launch velocity, decay, cancellation, stale-frame rejection, immediate inward escape, and outer-target safety. A compact native-touch check proves post-release pan continuation. A reduced-motion check proves release adds no movement. All checks retain 30,000 semantic stars and report no browser errors. These are behavioral checks, not timing benchmarks.

Outward zoom is constrained by projection math and fitted galaxy geography rather than a fixed magic scale. The camera derives a clip-safe reserve from viewport height, field of view, semantic minimum depth, and the shared 20,000-unit far distance. It uses 82 percent of that depth for the semantic plane and reserves the remainder for the decorative starfield. A separate viewport-derived envelope places the outer interaction target two percent beyond the padded fitted overview and begins resistance at 1.8 times fitted scale. Outward wheel, native touch, Mac Safari gesture, keyboard input, and trackpad momentum enter a 0.15-power rational curve whose derivative matches normal zoom at the boundary, then brakes decisively as the camera approaches the useful fitted target asymptotically. There is no reachable terminal coordinate or abrupt camera stop. Every inward ratio applies ordinary multiplicative zoom immediately, including the first event or momentum frame that leaves maximum overview. Keyboard and native two-touch pinch retain their exact inward ratios. Safari still uses the ratio between consecutive cumulative gesture values, so direction changes respond before the current gesture ends. Fit galaxy and resize recompute the envelope from current bounds while the deeper clip reserve remains unreachable through ordinary navigation. Every focal zoom continues to preserve its world point.

Inward zoom derives its maximum from viewport height, maximum semantic `z`, and a 96-unit near-camera clearance. This prevents compact canvases from crossing through prominent stars even under extreme input. Programmatic focus solves the real perspective projection for the target's `x`, `y`, and `z` against a bounded usable rectangle. A compact focus centers above the control sheet. A wide focus centers beside the left control panel. The same math supports the product sidebar and detail rail while the render canvas continues underneath them. Focus selects through the sparse interaction path and leaves resident star uploads unchanged.

Initial framing is distinct from the explicit Fit galaxy command. A compact canvas opens at no less than 0.16 scale around the semantic center, so the central identity field is immediately legible and provider geography remains discoverable beyond the viewport. Wider canvases retain a useful fitted scale or a minimum of 0.08. Fit galaxy still reveals the complete provider universe at any viewport size. Resize preserves this useful initial frame only until the operator moves the camera.

Labels and avatars use separate presentation lifecycles. Overview keeps stable provider and galaxy labels. Middle and close detail retain a bounded prominence-ranked label candidate pool. Every changed camera frame projects that pool through the actual perspective transform, applies the active zoom-density cap, and performs collision admission in screen space. Product scenes resolve only real nodes and labels returned by the coalesced worker presentation lane, so product presentation never scans resident people or invents missing identity copy on the main thread. Text rasterization remains unchanged while the camera moves. The accepted instance roster uploads only when its IDs change. Avatars remain settle-gated because image decode and atlas admission are not gesture work. Their cache is keyed by the accepted visible identity roster, so a close-range pan admits newly visible images after settle while retaining prior hits. Offscreen global favorites no longer consume the close-detail atlas.

The focused canvas also supports arrow-key pan, centered plus and minus zoom, Home or zero to fit, and Escape to clear selection. The viewport exposes those shortcuts through accessibility metadata and uses the active theme selection color for its focus ring. A live keyboard probe moved and zoomed the camera without world-buffer uploads, restored fit, selected a channel, and cleared that channel plus its contextual edge. Ambient motion defaults off when the device requests reduced motion, but the explicit laboratory preference remains available.

The original `Animate` control was misleading because its visible effect varied by backend. One renderer barely twinkled, one only faded close-detail icons, and one kept submitting static frames. The shared contract now names the feature `Ambient motion` and gives every GPU renderer the same settled-only stellar behavior: selective scintillation and depth-sensitive decorative dust drift. Raw WebGPU and WebGL2 also animate their provider field geometry. A camera gesture freezes these cosmetic branches while preserving every label and semantic star. Disabling motion or requesting reduced motion produces a static scene. Each frame updates only scalar shader uniforms. No scene walk, point-buffer write, label collision pass, atlas build, or React state update is allowed. Diagnostic schema 2 records the active state and renderer-specific profile.

The laboratory now exposes exactly one focusable graph region with one hidden description and one polite atomic live region. Every renderer canvas is `aria-hidden`, and semantic stars remain GPU instances rather than DOM nodes. The description names the selected identity or channel, available camera shortcuts, and reduced-motion state. Programmatic `focusNode` moves DOM focus to the graph and announces the synthesized locale-formatted label even when that node is outside the worker's compact metadata sample. Pointer or touch selection announces only on an actual selection boundary. Hover, camera motion, and ordinary settle do not write the live region. Escape announces selection clear. Backend recovery announces the compatibility renderer without exposing device-error text, while complete renderer failure receives one bounded unavailable message. The visible diagnostic status is no longer a competing live region, and clipboard outcomes use the same bounded announcer. Production cutover must add details and context-menu focus behavior in the React overlay without adding a per-star accessibility tree.

The laboratory now also separates the full-canvas render host from the bounded focus and input region. One geometry snapshot converts client input into canvas coordinates and derives camera insets from the intersection of those two rectangles. Backend resize, labels, avatars, picking, pinch anchoring, pointer selection, Safari gestures, fit, keyboard zoom, and programmatic focus all use full-canvas coordinates. Fit and focus center inside the usable interaction lane while stars continue beneath the left control surface. A coalesced observer refreshes both rectangles only when geometry changes. When responsive chrome changes the lane, one allocation-free reanchor keeps the same world point beneath the old and new usable centers without rebuilding the scene. Desktop browser proof used a 1,280-pixel canvas and a 924-pixel interaction lane beginning at pixel 356. The fitted galaxy center landed at pixel 818, exactly matching the usable-lane center, and a focused identity remained pickable through the offset. Resizing that focused state to 390 by 844 pixels preserved the selected identity and moved the same galactic-plane point exactly to the new center at 195 by 422 pixels while retaining all 30,000 semantic stars. Raw WebGPU, Three.js WebGPU, and WebGL2 each retained the same geometry and one hidden canvas. Simulated Raw WebGPU loss recovered to WebGL2 while preserving selection, the 356-pixel offset, and the bounded recovery announcement.

The engine-to-overlay boundary now emits stable, renderer-neutral requests instead of owning product menus. Right click or long press returns a node ID, request source, full-canvas point, bounded interaction anchor, and galactic-plane coordinates for the existing `Pin here` callback. Shift plus F10 and the Context Menu key project the selected star into the same bounded target path. Enter emits a details request for the selected node. The React shell can use those records to render the existing searchable person picker, pin command, promotion commands, and details surface without receiving camera movement state. The one-touch long-press tracker opens after 480 milliseconds, uses the same four-pixel movement boundary as tap selection, suppresses camera motion while pending or activated, and cancels on movement, release, cancellation, or a second touch. It never mutates scene positions and cannot reintroduce drag-to-pin or drag-to-link. Live browser proof produced one stationary long-press request with an unchanged transform, then produced no request after threshold movement or two-touch cancellation. Pointer, keyboard, and long-press requests all resolved to the same identity and bounded anchor.

Both WebGPU candidates use one whole-label canvas atlas and one GPU billboard batch. Raw WebGPU separates the bounded rasterized candidate pool from its visible instance roster. Labels remain visible throughout active camera movement, and the accepted roster follows the current camera frame instead of waiting for settle. Deterministic screen-density rules retain provider labels, spread identity labels across the viewport, and cap mobile detail more tightly than desktop detail.

Both WebGPU candidates include one bounded resident avatar atlas pass. Startup prepares a transparent roster from the worker's compact atlas metadata, so it does not scan the complete resident scene. The pass contributes no draw while opacity is zero. Camera scale drives a smooth target across a wide detail band, and an elapsed-time controller approaches that target without a tier switch. Raw WebGPU writes only a dedicated four-float opacity uniform and selects its already recorded avatar bundle when the value becomes visible. The Three.js reference writes the equivalent TSL uniform on its retained batch. The demand loop continues only while the fade remains active. Reduced motion and disabled animation settle immediately. A close settle may replace the startup roster with perspective-visible identities, then fades a changed roster from transparent. Camera motion never selects candidates, rebuilds the atlas, decodes images, changes labels, or writes stars. The atlas admits at most twelve identities on desktop and six on compact screens, always retains the selected identity, and resolves a selected linked channel to its parent identity. The label atlas treats avatars as collision obstacles and always retains the selected identity name outside the normal label sample.

The detached lab now proves the decoded-image path without remote media. It encodes deterministic local PNG blobs, decodes at most three images concurrently, admits no more than the close-tier identity cap, keeps at most eighteen source revisions, caches failures by source revision, and closes every evicted or disposed bitmap. A newer gesture, resize, backend switch, or detail transition invalidates the pending application, so a stale completion cannot rebuild the atlas during movement. The raw and Three.js WebGPU backends both accept the resulting image map. Overview reports zero decoded work. Settled desktop close detail admits twelve of twelve images, compact close detail admits six of six, and theme changes preserve the decoded atlas. Product integration must supply approved product avatar candidates through this same bounded contract.

Admission is retained by backend instance, visible versus close detail, compact width class, and selected person. An already applied key returns without selecting candidates, consulting the decode queue, replacing the image map, or rebuilding GPU avatar resources. A matching in-flight request advances to the latest settle generation instead of starting duplicate work. A stale completion can discard only its own pending key, so it cannot cancel or overwrite a newer backend, width class, or selection. The lab reports atlas apply and admission reuse counts. Product source revisions must join this key when real avatar candidates replace the fixed local fixture.

Selection and hover use a compact adjacency index. The engine writes only the previously active and newly active star instances instead of replacing complete color and emphasis buffers. Contextual links contain only the selected or hovered identity system, so the common fixture produces four short local links rather than a global edge field. The fixture worker builds a transferable sparse world-space pick grid in two typed-array passes. A pointer query derives the narrow world corridor under the locked perspective camera, visits only occupied intersecting cells, and then applies the exact depth, screen-radius, and prominence test to those candidates. It does not project the complete semantic scene. Empty space and distant pinned outliers do not allocate dense cell ranges. Raw and Three.js WebGPU diagnostics expose the latest candidate count against the source count.

The worker records the maximum adjacency degree beside its transferable offsets and indices. The scene index uses that scalar to allocate one interaction state, one role map, and one contextual-edge buffer for its lifetime without scanning the complete adjacency table during renderer startup. Each focus change mutates only the buffer's active prefix and count. The Three.js reference allocates its maximum edge geometry and sparse attribute-update records at startup, then mutates those buffers and retained changed-index sets in place. The current WebGL2 fallback restores and reapplies flags through one retained touched set.

Raw WebGPU uses a different interaction strategy. Its base semantic stream remains unchanged during hover and selection. A fixed-capacity CPU array copies the current roles into contiguous overlay instances, applies size and theme-selection mix there, clears unused slots, and performs one queue write. The backend then switches its one-element bundle list between the pre-recorded base and interactive bundles. Contextual edges remain one additional bounded write. Clearing selection writes zeroed overlay slots once and returns to the base bundle. Repeated hover no longer creates interaction maps, edge arrays, temporary set unions, typed views, replacement geometry, or scattered semantic-buffer writes.

Verification commands:

```bash
npm run typecheck:friends-galaxy-lab
npm run test:friends-galaxy-lab
npm run friends:galaxy-lab -- --port <free-port>
```

Append `?compact=1` to constrain the laboratory renderer to a real 390-pixel canvas. Add `&dpr=3` to exercise the iPhone render-density policy on a non-Retina host. Add `&controls=hidden&motion=0` for an unobstructed static compact visual probe. The legacy `animate=0` parameter remains accepted by old preview links. These parameters change only the detached laboratory shell and activate the same compact label and avatar admission branches used by a narrow product viewport.

The isolated compiler and all 239 deterministic fixture, product-engine, product-presentation, product-worker, product-worker-client, worker-loading, worker-transfer, envelope-admission, interaction-index, interaction-overlay, interaction-contract, star-instance, star-geometry, star-palette, camera-matrix, navigation-controller, motion-uniform, ambient-motion, initial-framing, gesture-math, inertial-pan, inertial-zoom, pointer-roster, viewport-geometry, frame-loop, render-density, diagnostic-ring, diagnostic-export, long-task-monitor, accessibility, settle-scheduler, backend-health, backend-runtime, provider-field, adjacency, picking, label-density, avatar-admission-state, avatar-image-admission, activity-index, activity-scene-patch, and local-orbit tests pass. Indexed picking matches the former exhaustive result across representative cameras and screen positions, and a distant pinned outlier does not inflate the sparse index. The interaction-index suite also proves that focus changes retain the exact state object, role map, and contextual-edge backing buffer while changing only the live count and contents. A tenfold represented-item increase with an unchanged summary roster produces the same twenty-one transfer buffers and exact byte lengths, while only summary-derived values and validated diagnostic scalars change. Desktop and iPhone-sized browser checks confirm nonblank output, worker-only startup, useful compact initial framing, compact metadata admission, direct worker-packed star uploads, constant-work theme switching, bounded spatial picking, backend switching, persistent gesture labels, sparse selection updates, short contextual links, selected avatar naming, distinct provider geometry, camera-scale field fading, bounded decoded-avatar admission, runtime device-loss recovery, and warning-free WebGPU pipeline creation. Sustained outward zoom remains above the fitted target and clip reserve in Raw WebGPU, Three.js WebGPU, and WebGL2. The first inward Safari gesture and control-wheel event advances at ordinary multiplicative speed without changing resident-star uploads. The full-canvas topology probe proved exact dual-rectangle geometry, usable-lane fit centering, offset pointer picking, hidden renderer canvases, responsive center reanchoring, selection preservation, and compact zero-inset collapse. The interaction-contract probe proved stable pointer, keyboard, details, and long-press requests plus exact movement and multi-touch cancellation. The presentation-lifecycle probe suspended an animating Raw WebGPU scene with a pending long press, stopped the frame loop and all settle work, left buffer, label-atlas, avatar-atlas, and resident-star upload counters unchanged, then resumed the same canvas, backend, camera, selection, and 30,000 semantic stars. Three.js WebGPU and WebGL2 preserved the same canvas, camera, selection, and semantic scene across the same cycle. The accessibility probe focused a semantic identity outside compact metadata, announced its synthesized label without its ID, left the announcement unchanged during a confirmed hover, cleared through Escape, reflected reduced motion, hid both Raw WebGPU and recovered WebGL2 canvases, and kept all semantic stars out of the DOM. During the mobile pinch probe, all eight overview labels remained present on every sampled gesture frame. The settled middle tier then admitted twenty labels. The close tier admits twelve decoded avatars on desktop and six on compact canvases, and keeps the selected name clear of the avatar rim in one additional draw. Live Raw and Three.js WebGPU checks retained the 30,000 semantic stars, 100,000 background stars, eleven overview labels, and four-link hovered identity system after the retained-interaction change. The Three.js reference also retained selection through a theme switch. A rebuilt Raw WebGPU drag kept the same 30,000 semantic stars, eleven labels, and every interaction layer while selecting a retained motion bundle with 50,000 decorative stars, then restored all 100,000 decorative stars at settle. The raw overlay check moved from eight idle uploads to ten on hover, exactly one interaction stream and one edge stream, while resident-star uploads stayed at two. Selection survived a Neon switch, and clearing it returned from six draws to the four-draw base bundle with one zeroing upload. All four active laboratory themes produce distinct nonblank output in Raw WebGPU, Three.js WebGPU, and WebGL2 through the shared parser. Raw WebGPU keeps resident-star uploads at two throughout every switch. Programmatic focus places positive-depth targets at the exact bounded usable center on both compact and wide canvases while resident-star uploads stay at two. The live worker receipt reports 25,000 activity summaries, 250,000 represented items, 30,000 semantic stars, two resident star uploads, and twenty-one transferred buffers. A live copy emitted a bounded versioned snapshot with no identity or content keys. Forced clipboard rejection produced one bounded error state and restored the button. Chromium exposed the passive long-task monitor consistently through visible diagnostics, inspection state, and the copied schema. These are functional checks only. Their frame timing and observed long-task values are not benchmark evidence while unrelated machine workloads remain active.

### Laboratory backend decision

Raw WebGPU is selected as the primary shared renderer. It handles semantic stars, decorative stars, one procedural field batch, billboard labels, contextual links, picking, and sparse interaction in four overview draws. It uses a dedicated allocation-free camera matrix instead of a Three.js runtime dependency, preserves light-theme output more faithfully, and gives exact control over buffer writes and shader behavior. The current WebGL2 engine remains the deterministic compatibility fallback for devices without usable WebGPU. Three.js WebGPU remains a maintained laboratory reference until production cutover proves that it no longer provides a required compatibility advantage.

This decision does not authorize a runtime cutover by itself. The remaining gates are production avatar candidate plumbing, product integration behind the imperative engine contract, every approved Friends workflow, repeated installed Freed Desktop consistency ranges, physical iPhone Safari validation, fallback validation, integrated accessibility and menu workflows, and final owner visual acceptance. Native Metal remains unnecessary unless those repeated device results show that the shared WebGPU path cannot meet the approved budgets.

Explicit laboratory switches also admit both compatibility paths. The current WebGL2 backend keeps all 30,000 semantic stars, applies its established bounded decorative field, switches themes, and disables unsupported raw-field controls. The Three.js WebGPU reference accepts the equal 30,000 plus 100,000 workload and also disables raw-only controls. Both initialize without browser warnings. Initialization failure in either WebGPU path routes automatically to the current WebGL2 backend with the failure reason preserved in the status surface.

Runtime device loss now follows the same deterministic route through `packages/ui/src/lib/friends-galaxy-backend-runtime.ts`. Each WebGPU backend reports its first fatal device error through a one-shot health signal and ignores intentional teardown. The shared runtime stages a hidden compatibility candidate while leaving the last visible canvas in place, applies current scene, theme, camera, and interaction state before atomic commit, and ignores superseded generations. It retains an already healthy compatibility backend when a preferred switch fails. A compatibility runtime failure latches terminal state and removes that backend from the demand-frame predicate, so dirty state cannot create a recovery or scheduling loop. Diagnostics expose the activation generation, pending recovery, terminal latch, and bounded reason. Live forced-loss checks retained Neon, the selected channel, camera scale, all 30,000 semantic stars, and one visible canvas after WebGL2 recovery with no browser logs. Installed-device loss and resume behavior remain production acceptance gates.

The laboratory startup path now compiles its complete 30,000-star semantic fixture and interaction index in a dedicated one-shot worker. Twenty-one numeric buffers transfer ownership to the main thread, including positions, visual channels, indexed edges, decorative stars, the stable-id hash table, adjacency offsets, sparse pick-cell slots, coordinates, offsets, node indices, and two direct-upload star instance streams. The UI receives only 192 rich node records plus bounded label and provider metadata. Renderer switching reuses the transferred hash, adjacency tables, and spatial pick grid, so it does not rebuild graph indexes. Worker timeout or failure is explicit and terminal for the laboratory startup. There is no whole-graph main-thread fallback.

The loader admits that worker envelope before creating any renderer. Its constant-time validation checks the top-level response kind and request id, scene version, scalar source counts, typed-array classes and expected lengths, scene and pick bounds, adjacency and pick-offset terminal values, packed star stream sizes, rich-metadata cap, and exact receipt counts. It does not scan node ids, star instances, edges, or pick entries. A malformed, mixed-version, or non-deserializable response closes the one-shot worker immediately and surfaces one explicit startup error instead of passing bad bytes into WebGPU, falling back into a main-thread rebuild, waiting for the timeout, or retaining a partially trusted scene.

Renderer-scene assembly is now shared UI code rather than a laboratory recipe. One deterministic compiler accepts the semantic scene, atlas metadata, structural source counts, and decorative-star budget, then builds the sparse interaction index, background field, and direct-upload star streams. One shared metadata compactor retains priority selections, required label records, and a bounded identity sample while dropping rich edges and hit buckets. A separate product-scene compiler consumes the real atlas model, excludes provider aggregates from semantic GPU residency, derives people, channel, and linked-channel counts from semantic records, compiles settled visual channels, preserves selected identity metadata, and emits the same bounded renderer envelope. The laboratory worker consumes the lower-level functions directly, while the worker-envelope suite routes product-shaped `Person` and `Account` records through the product compiler and admits the exact 21-buffer transfer contract without copying fixture code.

Viewport presentation is a separate bounded lane. Its payload is capped at 192 rich nodes, 120 labels, and 16 provider regions, with no edges or hit buckets. The shared admission check validates bounded strings, numeric geometry, unique ids, source metrics, and every semantic node or label id against the resident hash index. Provider aggregates must match an admitted provider region. During motion, the renderer host applies admitted candidate metadata without replacing the settled camera, semantic stars, background, interaction, edges, or avatars. Settle records the final transform and may admit a new avatar roster. Preferred-backend activation and WebGL2 recovery initialize from the latest admitted viewport presentation rather than the startup slice.

The real product worker boundary uses a versioned three-lane protocol. The source lane overwrites caller layout dimensions with one 1,400 by 900 virtual plane, builds and caches the real atlas model by source revision, compiles the resident renderer scene, builds stable social and RSS activity bindings, and transfers its exact twenty-one numeric buffers once. The viewport-presentation lane requires that cached revision, slices against the current transform and viewport, prioritizes selected metadata, caps rich nodes at 192, and returns no scene transfer, edges, or hit buckets. The sparse-activity lane requires the same revision and converts only changed source summaries into six sorted typed node buffers plus bounded unknown-source keys. Product responses are admitted against the resident scene and sparse index before the renderer host sees them. Product scenes carry an atlas-candidate policy, so label and avatar selection iterates only admitted metadata. A laboratory scene may retain its resident-candidate policy for synthetic stress exploration. No main-thread compiler fallback exists in this boundary.

One shared product worker client owns the browser port around that protocol. Starting a source revision invalidates and terminates the prior worker generation, but does not tell the renderer host to discard its last-good scene. A presentation request received before source admission replaces the prior queued request. After admission, at most one presentation request is posted while one latest request remains queued. The input controller samples a changed camera no more often than every 72 milliseconds, and the renderer performs current-camera label projection between worker replies. Sparse activity uses an independent one-in-flight lane. Its queued revisions merge by source key so the latest same-source update wins without dropping unrelated changes, and replies apply in revision order. Source, presentation, and activity deadlines are independent scalars consumed by the existing health poll. Source changes, old messages, and old errors are generation-isolated. Invalid current messages, wrong lanes, worker errors, deserialization failure, posting failure, or a lane deadline terminate the active port with one bounded failure record. The client exposes no compiler fallback and creates no browser movement timer.

The product presentation resolver owns one stable callback and a map containing only the current capped atlas metadata. Source and viewport-presentation worker lanes build their complete metadata and account-parent indexes once with the cached model. A selected linked channel explicitly admits its parent identity before compaction, even when viewport culling would otherwise omit that person. Renderer label and avatar resolution then reads real product labels, initials, priority, and avatar URLs from at most 192 admitted records. Missing metadata fails closed instead of synthesizing product copy or scanning the resident scene.

One imperative product engine composes the worker client, real presentation resolver, and retained renderer host. State set before source admission, including dimensions, density, palette, motion, field style, interaction, settled camera, and activity patches, replays before the first candidate canvas becomes visible. A source replacement invalidates an older candidate even when that candidate is still initializing. Viewport metadata and activity patches are forwarded only when the visible backend owns the same semantic scene. Motion metadata refreshes only the bounded candidate presentation. Settle additionally records the final detail and camera state. If either arrives during replacement, the old canvas remains internally coherent and the candidate receives the latest bounded presentation plus cumulative typed activity state before commit. Ordinary live activity still applies only its sparse batch. Unknown source keys remain visible to the product shell so it can request a structural refresh. Worker failure terminates only the failed worker generation and deliberately leaves the last visible renderer plus admitted metadata intact. Engine state reports that admitted scene separately from the current worker-port state and requested revision, so diagnostics can distinguish a preserved graph from a healthy compilation lane.

That engine now also composes the shared navigation controller as soon as the first source scene is admitted. Viewport dimensions and full-background interaction insets may arrive before source admission and replay into the first camera. The engine exposes fit, stable-ID focus, pan, fixed-focal zoom, moving-focal zoom, pinch, camera rendering, settled detail, and a camera-derived presentation request. Source replacement preserves the world point under the usable center and clamps it above the soft outward target. The first source seeds a settled transform before renderer activation. The detached laboratory routes the same operations through the same controller and no longer writes transform coordinates directly.

Product interaction is deliberately not a fourth worker protocol. Pointer hover, touch selection, search focus, Reader focus, and keyboard selection already produce stable node IDs. The product engine sends that two-ID state directly to the renderer host, where the shared scene interaction index patches only the active identity system and contextual edge buffer. No source, presentation, or activity message is posted. The latest interaction state is replayed into a switched or recovered backend before its hidden canvas commits, so the direct path preserves atomic replacement without adding worker latency to the hottest user action.

The worker-packed semantic and decorative streams are theme-neutral. Each instance carries position, size, brightness, palette role, interaction mix, and alpha scale. Raw WebGPU uploads those streams once. A shared structural palette contract owns normalized color conversion, light-surface classification, role alpha, the fixed uniform offset, and all eleven active-theme colors. Laboratory themes extend that contract directly, and every backend imports the same parser. Raw WebGPU theme changes therefore write only the bounded uniform block, with no laboratory adapter, resident-star loop, or reupload. Live Scriptorium-to-Neon verification kept the resident star upload counter at two while visibly changing the complete field, preserving 30,000 semantic stars, selection, and contextual links.

The product cutover removes those three hazards. `FriendGraph.tsx` no longer imports or calls `runAtlasOnMainThread`, no longer owns the old atlas worker or `IdentityGalaxyEngine`, and no longer shifts frame-history arrays or recreates browser settle timers during movement. The product worker fails closed while the renderer retains its last-good scene, fixed sample rings own frame evidence, and the scalar settle scheduler drives bounded post-motion presentation refresh.

## Incremental Activity Index Checkpoint

The exact activity-summary index now lives under shared UI source at `packages/ui/src/lib/friends-galaxy-activity-index.ts`, with its sparse scene encoder beside it at `packages/ui/src/lib/friends-galaxy-activity-patches.ts`. The detached laboratory imports both modules directly. The index deliberately retains state proportional to social and RSS source count rather than feed-item count. Each source bucket holds only total and location counts, the latest three sample candidates, and the latest three distinct avatar candidates. Snapshot records use null prototypes and clone their bounded arrays so hostile source keys and external mutation cannot alter retained state. Scene bindings reject invalid 32-bit indices and prevent one semantic node from inheriting conflicting activity sources.

Normal additions, updates that do not replace a leading candidate, and removals outside the bounded candidates produce key-level summary patches without a library scan. If a destructive mutation removes a leading sample or avatar candidate, the index invalidates only that source. The owning document worker performs one resolver pass over its current authoritative document and rebuilds only the invalidated source buckets. No rebuild, sort, or item traversal occurs on the React thread.

The deterministic stress test represents 250,000 feed items across 25,000 source summaries. A subsequent addition changes one summary patch, reports zero rebuilt sources, and leaves item payloads out of the graph fixture. This is structural correctness evidence, not a timing benchmark.

The runnable worker fixture now carries that source scale through its admitted envelope. Its receipt binds 25,000 activity summaries and 250,000 represented items to the 30,000-star semantic scene while retaining the same twenty-one numeric transfers. A paired fixture test increases represented item volume tenfold with the person, account, summary, and background rosters held fixed. Transfer count and every buffer byte length remain identical. The represented count is a diagnostic scalar, not an item array, and summary item counts are consumed once while compiling bounded channel activity values.

The detached laboratory also includes an activity-to-scene patch encoder. It binds social external IDs and RSS URLs to stable semantic node indices once, then converts summary changes into sorted `Uint32Array` node indices, item counts, `Float64Array` activity timestamps, deterministic `Float32Array` size and brightness scales, one byte of location, avatar, or removal flags, and at most one avatar URL per affected node. The visual scales use an explicit reference timestamp, bounded count and recency signals, and a small location boost, so worker output is deterministic and testable. A source represented by more than one semantic node fans out deterministically. An unknown source is reported separately so the atlas worker can make a structural decision. No positions, labels, sample item IDs, or feed-item records enter this payload. A 25,000-binding test proves that one changed source produces one encoded node entry. The runnable laboratory executes the same path at startup, applies that one node directly to the raw WebGPU semantic buffer, and reports one summary key, one scene node, one GPU activity node, and zero unknown keys in its diagnostics.

### Production activity protocol

1. Reuse the shared UI contribution, summary, delta, index, and scene-patch modules in Freed Desktop and PWA. Do not fork or translate their source-key and typed-array contracts.
2. Let each Automerge worker own the index. Initial hydration builds it while the worker already traverses the authoritative feed-item document. Full document replacement, merge, reset, and schema recovery rebuild it in the worker.
3. At incremental mutation boundaries, derive previous and next contributions from the immutable document before and after `A.change()`. Attach source-key summary patches and a monotonic summary revision to the existing item-patch response.
4. Keep one derived activity bridge outside React. It applies key patches without cloning the complete 25,000-key snapshot and exposes a revision for subscribers. `FriendsView` subscribes to that revision and compact summary snapshot, never to the complete `items` array for graph or overview entry.
5. Build friend overview rows from persons, account indexes, and summaries. Avatar candidates, captured counts, recent activity, and location flags come from summaries rather than retained item arrays.
6. Add an on-demand worker query for the selected person or account timeline. Only the detail rail receives those full items. Selection changes cancel stale requests, and closing details releases the item array.
7. Precompute the bounded friend-candidate suggestion list in the document worker from content signals and contact overlap. Friends consumes the resulting top suggestions instead of rescoring every item on mount.
8. Give the graph atlas worker one initial compact summary snapshot and later source-key patches. Its source-to-scene encoder turns activity-only changes into sorted node-index typed patches for brightness, size, location, and avatar metadata without rebuilding stable galaxy coordinates or resending the complete semantic scene. Unknown source keys request a structural atlas refresh rather than being silently dropped.

This protocol removes the remaining `FriendsView` mount scans: feed-item record materialization, graph summary construction, source-item indexing, overview item grouping, candidate rescoring, and selected-account filtering. Account-to-person indexes also move off the React render path. Existing `Person` plus `Account` records and every explicit identity workflow remain unchanged.
