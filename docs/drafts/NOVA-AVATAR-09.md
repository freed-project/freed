# Nova avatar 09

Recommend the blue Cassiopeia A source at focal x 0.5, y 0.5, zoom 1. The lead already viewed the original. This review independently rendered and opened its actual 220-pixel and 48-pixel circles.

Evidence: `/tmp/freed-nova-09/avatar-circles.png`. Left pair is zoom 1; right pair is zoom 1.1. The complete broken shell and upper-left jet remain inside the centered circle. Bright rim, darker interior and roughly round blue silhouette remain legible at 48 pixels. The larger crop reveals internal strands. Zoom 1.1 gains little and brings the jet too close to the boundary.

The headless Playwright probe directly loaded the current `cropDecodedGalaxyAvatar` implementation from `packages/ui/src/lib/friends-galaxy-avatar-crop.ts`, stripped only TypeScript syntax, and used its clamped source crop and 256-pixel intermediate canvas. It displayed that canvas at 220 and 48 CSS pixels with circular clipping. This is an actual runtime crop-helper check, not a substitute formula. Browser closed afterward. It does not verify deployed avatar routing or WebGL delivery.

Original local file: `/tmp/freed-nova-review-08/replacement-1.jpg`. Original SHA-1: `9d5d5d332cb330e6582ed6cb4af44387a69f864e`. Dimensions: 576 × 576. Credit: NASA/CXC/GSFC/U.Hwang et al. Public domain confirmed by live Commons metadata in review 08. Exact source URL and focal metadata are in `NOVA-AVATAR-09.json`.

[Primary image description](https://chandra.si.edu/photo/2004/casa/more.html) identifies the expanding shock ring and jet. [Commons source](https://commons.wikimedia.org/wiki/File:Chandra_Broadband_Image_of_Cassiopeia_A_(2004-casa-more-4).jpg). This is a core-collapse remnant, consistent with the lead’s selected Cas A identity. No pre-explosion episode is admitted by approving its avatar.

No runtime changes or tracked image binaries.

The lead independently opened the rendered circles and approved the centered x 0.5, y 0.5, zoom 1 proposal. An encoded-color alt is included in the JSON. Runtime aggregation remains with the lead.
