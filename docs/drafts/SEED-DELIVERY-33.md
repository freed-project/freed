# Seed display delivery33

Replaced the two FWS seed originals in runtime with the exact Large download links published on their primary media pages. The lead opened both resulting files and confirmed the same framing, ordered seeds, and emerging silk. Narratives remain unchanged. No local image manipulation or invented derivative URL.

Packed carefully: 1,300 by 867 pixels, 663,578 bytes. One at a time: 1,300 by 868 pixels, 702,968 bytes. Both direct downloads returned HTTP 200 and image/jpeg. Combined compressed size is 1,366,546 bytes instead of 37,863,652 bytes, about 96.4 percent less. At four bytes per pixel the two decoded images total 9,022,000 bytes, excluding decoder and browser overhead.

Original attribution, public-domain status and primary source links remain intact. SEED-DELIVERY-33.json preserves the original and display identities, exact URLs, hashes and byte sizes. Source pages explicitly offer these variants. Catalog and entry counts are unchanged. Product still owns actual mobile Story and relay delivery verification.
