{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "main-logging",
  "description": "Write local main-window diagnostics through the native logger without granting access to provider pages or login windows.",
  "local": true,
  "windows": ["main"],
  "permissions": ["log:allow-log"]
}
