/**
 * Re-export the canonical CloudProviderCard from the shared @freed/ui package.
 * The component (and its types) live in packages/ui — do not duplicate them here.
 */
export {
  CloudProviderCard,
  type CloudProvider,
  type ProviderState,
  type CloudProviderStatus,
} from "@freed/ui/components/CloudProviderCard";
